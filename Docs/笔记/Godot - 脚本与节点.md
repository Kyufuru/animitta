链接: [[Godot]]

标签: #笔记 #Godot 

创建时间: 2023-12-16 11:34

---

- 所有的脚本都必须依附于节点运行
- 所有节点都是类的实例, 有默认和内置的属性/方法
- 内置属性/方法以 _ 开头
- 常用方法:
    `_ready()`: 当节点挂载到节点树时调用
    `_process(delta)`: 游戏内的每一帧都会调用, delta 为上一帧到当前帧的时长